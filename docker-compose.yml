services:
  ch8hubbuildai:
    build: .
    container_name: ch8hubbuildai
    restart: unless-stopped
    ports:
      - "8902:8902"
    env_file:
      - .env.docker
    volumes:
      - ./projects:/app/projects
